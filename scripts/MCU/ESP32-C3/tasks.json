{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "(MCU:ESP32-C3/pio): Build and upload",
            "type": "shell",
            "command": "python crossplay.py run MCU:ESP32-C3",
            "problemMatcher": ["$gcc"],
            "options": {
                "cwd": "${workspaceFolder}"
            },

            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        
        {
            "label": "(MCU:ESP32-C3/pio): Build",
            "type": "shell",
            "command": "python crossplay.py run MCU:ESP32-C3 --build-only",
            "problemMatcher": ["$gcc"],
            "options": {
                "cwd": "${workspaceFolder}"
            },

            "group": "build"
        }
    ]
}